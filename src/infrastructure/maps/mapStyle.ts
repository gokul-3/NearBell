// OpenFreeMap: no API key, no account, no request limits, commercial use
// explicitly allowed — https://openfreemap.org. Attribution is required;
// the style JSON's own source attribution is shown via <Map attribution />
// (MapLibre's built-in attribution control), which must stay enabled.
export const MAP_STYLE_URL = 'https://tiles.openfreemap.org/styles/liberty';
